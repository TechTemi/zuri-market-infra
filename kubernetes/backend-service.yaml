apiVersion: v1
kind: Service
metadata:
  name: zuri-backend-service
  namespace: zuri-market
  labels:
    app: zuri-backend
spec:
  type: ClusterIP
  selector:
    app: zuri-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
