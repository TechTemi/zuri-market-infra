apiVersion: v1
kind: Service
metadata:
  name: zuri-frontend-service
  namespace: zuri-market
  labels:
    app: zuri-frontend
spec:
  type: NodePort
  selector:
    app: zuri-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
